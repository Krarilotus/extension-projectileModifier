{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Custom Projectiles preset",
  "type": "object",
  "additionalProperties": false,
  "description": "Projectile preset. All unit entries and settings are optional; omitted settings preserve native behavior or documented automatic-fire defaults. UCP required/suggested qualifiers apply to the file selector, not fields inside this file. See README.md.",
  "properties": {
    "decorations": {
      "type": "object",
      "maxProperties": 33,
      "additionalProperties": false,
      "patternProperties": {
        "^[a-z][a-z0-9_-]{0,47}$": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "label": {
              "type": "string",
              "minLength": 1,
              "maxLength": 96
            },
            "sprites": {
              "type": "string",
              "minLength": 5,
              "maxLength": 240,
              "pattern": "\\.[gG][mM]1$",
              "description": "Optional complete body_brazier sheet: 8 images, GM1 type 6. Omission uses native brazier graphics. Build from the brazier button."
            }
          }
        }
      }
    },
    "projectiles": {
      "type": "object",
      "maxProperties": 33,
      "additionalProperties": false,
      "propertyNames": {
        "not": {
          "enum": [
            "arrow",
            "arrow_untargeted",
            "ballista_bolt",
            "catapult_rock",
            "cow",
            "crossbow_bolt",
            "crossbow_bolt_untargeted",
            "fire_arrow",
            "fire_arrow_untargeted",
            "fire_ballista_bolt",
            "firethrower_pot",
            "firethrower_pot_untargeted",
            "mangonel_pebble",
            "slinger_stone",
            "slinger_stone_untargeted",
            "trebuchet_rock"
          ]
        }
      },
      "patternProperties": {
        "^[a-z][a-z0-9_-]{0,47}$": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "inherits",
            "sprites"
          ],
          "properties": {
            "inherits": {
              "enum": [
                "arrow",
                "arrow_untargeted",
                "ballista_bolt",
                "catapult_rock",
                "cow",
                "crossbow_bolt",
                "crossbow_bolt_untargeted",
                "fire_arrow",
                "fire_arrow_untargeted",
                "fire_ballista_bolt",
                "firethrower_pot",
                "firethrower_pot_untargeted",
                "mangonel_pebble",
                "slinger_stone",
                "slinger_stone_untargeted",
                "trebuchet_rock"
              ]
            },
            "sprites": {
              "type": "string",
              "minLength": 5,
              "maxLength": 240,
              "pattern": "\\.[gG][mM]1$",
              "description": "GM1 path relative to the game folder. Use a complete matching base sheet; see README for counts and formats."
            }
          }
        }
      }
    },
    "units": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "Peasant": {
          "$ref": "#/$defs/unit"
        },
        "Burning man": {
          "$ref": "#/$defs/unit"
        },
        "Woodcutter": {
          "$ref": "#/$defs/unit"
        },
        "Fletcher": {
          "$ref": "#/$defs/unit"
        },
        "Tunneler": {
          "$ref": "#/$defs/unit"
        },
        "Hunter": {
          "$ref": "#/$defs/unit"
        },
        "Quarry mason": {
          "$ref": "#/$defs/unit"
        },
        "Quarry grunt": {
          "$ref": "#/$defs/unit"
        },
        "Quarry ox": {
          "$ref": "#/$defs/unit"
        },
        "Pitch worker": {
          "$ref": "#/$defs/unit"
        },
        "Wheat farmer": {
          "$ref": "#/$defs/unit"
        },
        "Hops farmer": {
          "$ref": "#/$defs/unit"
        },
        "Apple farmer": {
          "$ref": "#/$defs/unit"
        },
        "Dairy farmer": {
          "$ref": "#/$defs/unit"
        },
        "Miller": {
          "$ref": "#/$defs/unit"
        },
        "Baker": {
          "$ref": "#/$defs/unit"
        },
        "Brewer": {
          "$ref": "#/$defs/unit"
        },
        "Poleturner": {
          "$ref": "#/$defs/unit"
        },
        "Blacksmith": {
          "$ref": "#/$defs/unit"
        },
        "Armourer": {
          "$ref": "#/$defs/unit"
        },
        "Tanner": {
          "$ref": "#/$defs/unit"
        },
        "European archer": {
          "$ref": "#/$defs/unit"
        },
        "European crossbowman": {
          "$ref": "#/$defs/unit"
        },
        "European spearman": {
          "$ref": "#/$defs/unit"
        },
        "European pikeman": {
          "$ref": "#/$defs/unit"
        },
        "European maceman": {
          "$ref": "#/$defs/unit"
        },
        "European swordsman": {
          "$ref": "#/$defs/unit"
        },
        "European knight": {
          "$ref": "#/$defs/unit"
        },
        "Ladderman": {
          "$ref": "#/$defs/unit"
        },
        "Engineer": {
          "$ref": "#/$defs/unit"
        },
        "Iron miner1": {
          "$ref": "#/$defs/unit"
        },
        "Iron miner2": {
          "$ref": "#/$defs/unit"
        },
        "Priest": {
          "$ref": "#/$defs/unit"
        },
        "Healer": {
          "$ref": "#/$defs/unit"
        },
        "Drunkard": {
          "$ref": "#/$defs/unit"
        },
        "Innkeeper": {
          "$ref": "#/$defs/unit"
        },
        "Monk": {
          "$ref": "#/$defs/unit"
        },
        "unknown1": {
          "$ref": "#/$defs/unit"
        },
        "Catapult": {
          "$ref": "#/$defs/unit"
        },
        "Trebuchet": {
          "$ref": "#/$defs/unit"
        },
        "Mangonel": {
          "$ref": "#/$defs/unit"
        },
        "Trader": {
          "$ref": "#/$defs/unit"
        },
        "Trader horse": {
          "$ref": "#/$defs/unit"
        },
        "Deer": {
          "$ref": "#/$defs/unit"
        },
        "Lion": {
          "$ref": "#/$defs/unit"
        },
        "Rabbit": {
          "$ref": "#/$defs/unit"
        },
        "Camel": {
          "$ref": "#/$defs/unit"
        },
        "Crow": {
          "$ref": "#/$defs/unit"
        },
        "Seagull": {
          "$ref": "#/$defs/unit"
        },
        "Siege tent": {
          "$ref": "#/$defs/unit"
        },
        "Cow": {
          "$ref": "#/$defs/unit"
        },
        "Hunter dog": {
          "$ref": "#/$defs/unit"
        },
        "Fireman": {
          "$ref": "#/$defs/unit"
        },
        "Ghost": {
          "$ref": "#/$defs/unit"
        },
        "Lord": {
          "$ref": "#/$defs/unit"
        },
        "Lady": {
          "$ref": "#/$defs/unit"
        },
        "Jester": {
          "$ref": "#/$defs/unit"
        },
        "Siege tower": {
          "$ref": "#/$defs/unit"
        },
        "Battering ram": {
          "$ref": "#/$defs/unit"
        },
        "Portable shield": {
          "$ref": "#/$defs/unit"
        },
        "Tower ballista": {
          "$ref": "#/$defs/unit"
        },
        "Chicken": {
          "$ref": "#/$defs/unit"
        },
        "Mother": {
          "$ref": "#/$defs/unit"
        },
        "Child": {
          "$ref": "#/$defs/unit"
        },
        "Juggler": {
          "$ref": "#/$defs/unit"
        },
        "Fireeater": {
          "$ref": "#/$defs/unit"
        },
        "Dog": {
          "$ref": "#/$defs/unit"
        },
        "unknown2": {
          "$ref": "#/$defs/unit"
        },
        "unknown3": {
          "$ref": "#/$defs/unit"
        },
        "Arabian archer": {
          "$ref": "#/$defs/unit"
        },
        "Arabian slave": {
          "$ref": "#/$defs/unit"
        },
        "Arabian slinger": {
          "$ref": "#/$defs/unit"
        },
        "Arabian assassin": {
          "$ref": "#/$defs/unit"
        },
        "Arabian horse archer": {
          "$ref": "#/$defs/unit"
        },
        "Arabian swordsman": {
          "$ref": "#/$defs/unit"
        },
        "Arabian firethrower": {
          "$ref": "#/$defs/unit"
        },
        "Fire ballista": {
          "$ref": "#/$defs/unit"
        }
      }
    }
  },
  "$defs": {
    "unit": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "attached_board_radius": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "attached_interval": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000,
              "description": "Enables firing for a siege tower docked to a wall, overriding moving/standing intervals; 0 holds fire. Omission keeps the current moving/standing rate or fallback."
            },
            {
              "const": "native"
            }
          ],
          "description": "Enables firing for a siege tower docked to a wall, overriding moving/standing intervals; 0 holds fire. Omission keeps the current moving/standing rate or fallback. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "count": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 64
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "cow_count": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 64
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "density_min": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 256,
              "description": "Minimum enemy count for AI cluster targeting; human attack orders are not restricted by this threshold."
            },
            {
              "const": "native"
            }
          ],
          "description": "Minimum enemy count for AI cluster targeting; human attack orders are not restricted by this threshold. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "density_radius": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 100,
              "description": "Radius in tiles for counting nearby enemies in an AI cluster."
            },
            {
              "const": "native"
            }
          ],
          "description": "Radius in tiles for counting nearby enemies in an AI cluster. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "inaccuracy": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 800,
              "description": "Maximum random aim-error radius in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Explicit 0 removes native random error; omission preserves it. Separate spread still applies. Do not combine with inaccuracy_tiles."
            },
            {
              "const": "native"
            }
          ],
          "description": "Maximum random aim-error radius in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Explicit 0 removes native random error; omission preserves it. Separate spread still applies. Do not combine with inaccuracy_tiles. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "inaccuracy_tiles": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer inaccuracy for native 1/8-tile steps. Do not combine both fields."
            },
            {
              "const": "native"
            }
          ],
          "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer inaccuracy for native 1/8-tile steps. Do not combine both fields. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "interval": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000,
              "description": "Optional fallback automatic-fire interval in simulation ticks. Applies only where no state interval overrides it."
            },
            {
              "const": "native"
            }
          ],
          "description": "Optional fallback automatic-fire interval in simulation ticks. Applies only where no state interval overrides it. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "interval_moving": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000,
              "description": "Enables firing while moving; 0 holds fire. If omitted, use interval, or hold fire if neither is set."
            },
            {
              "const": "native"
            }
          ],
          "description": "Enables firing while moving; 0 holds fire. If omitted, use interval, or hold fire if neither is set. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "interval_standing": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000,
              "description": "Enables firing while stopped, not specifically docked; 0 holds fire. If omitted, use interval, or hold fire if neither is set."
            },
            {
              "const": "native"
            }
          ],
          "description": "Enables firing while stopped, not specifically docked; 0 holds fire. If omitted, use interval, or hold fire if neither is set. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "preload_poll": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "range": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "require_manned": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 4
                },
                {
                  "type": "boolean"
                }
              ],
              "description": "Engineers currently aboard. Native reload defaults: trebuchet 3, other supported siege engines 2. Independent timer defaults 0. Explicit true means 1; false or 0 allows unmanned fire."
            },
            {
              "const": "native"
            }
          ],
          "description": "Engineers currently aboard. Native reload defaults: trebuchet 3, other supported siege engines 2. Independent timer defaults 0. Explicit true means 1; false or 0 allows unmanned fire. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "shoot_height": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 500
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "spread": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 800,
              "description": "Additional simultaneous-shot spread per axis, in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Zero adds no spread. Independent of inaccuracy."
            },
            {
              "const": "native"
            }
          ],
          "description": "Additional simultaneous-shot spread per axis, in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Zero adds no spread. Independent of inaccuracy. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "spread_tiles": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer spread for native 1/8-tile steps. Do not combine both fields."
            },
            {
              "const": "native"
            }
          ],
          "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer spread for native 1/8-tile steps. Do not combine both fields. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "stagger_max": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "stagger_min": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000,
              "description": "Requires stagger_max; must not exceed it when staggering is enabled. Checked by the game loader."
            },
            {
              "const": "native"
            }
          ],
          "description": "Requires stagger_max; must not exceed it when staggering is enabled. Checked by the game loader. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "sync_max_wait": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000,
              "description": "Legacy animation wait only; never bypasses a native reload firing frame."
            },
            {
              "const": "native"
            }
          ],
          "description": "Legacy animation wait only; never bypasses a native reload firing frame. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "wall_min_distance": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "ai_cow_vs_units": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "ai_only": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "attached_ignore_crew": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "attached_stop_when_boarded": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "auto_targeting": {
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "False requires an explicit human attack order and disables module automatic search and native automatic acquisition for this unit. True preserves native acquisition and permits configured interval searches. Native leaves the game unchanged when no interval is configured."
            },
            {
              "const": "native"
            }
          ],
          "description": "False requires an explicit human attack order and disables module automatic search and native automatic acquisition for this unit. True preserves native acquisition and permits configured interval searches. Native leaves the game unchanged when no interval is configured. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults.",
          "default": true
        },
        "preload": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "random_targets": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "strict_range": {
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Check automatic targets at native coordinate precision, including building centres. False restores the old rounded tile checks. Does not clamp projectile scatter or change native manual range rules."
            },
            {
              "const": "native"
            }
          ],
          "description": "Check automatic targets at native coordinate precision, including building centres. False restores the old rounded tile checks. Does not clamp projectile scatter or change native manual range rules. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults.",
          "default": true
        },
        "suppress_default": {
          "anyOf": [
            {
              "type": "boolean",
              "description": "Defaults true with an interval. False combines native and automatic shots only in independent timer mode; it cannot bypass native reload timing. Unchanged cow orders remain available."
            },
            {
              "const": "native"
            }
          ],
          "description": "Defaults true with an interval. False combines native and automatic shots only in independent timer mode; it cannot bypass native reload timing. Unchanged cow orders remain available. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "sync_to_animation": {
          "anyOf": [
            {
              "type": "boolean",
              "description": "Defaults to native firing-frame timing for catapult, trebuchet, mangonel, both ballistas, European/Arabian archers, horse archers, crossbowmen, slingers, firethrowers and hunters. Reload while waiting; never truncate the animation. Horse archers keep an independent bow clock while moving. Configured hunter shots use the native bow animation and retain other work states. False selects the independent timer. Other units default false; true uses the legacy bounded animation wait."
            },
            {
              "const": "native"
            }
          ],
          "description": "Defaults to native firing-frame timing for catapult, trebuchet, mangonel, both ballistas, European/Arabian archers, horse archers, crossbowmen, slingers, firethrowers and hunters. Reload while waiting; never truncate the animation. Horse archers keep an independent bow clock while moving. Configured hunter shots use the native bow animation and retain other work states. False selects the independent timer. Other units default false; true uses the legacy bounded animation wait. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "turn_before_shot": {
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Turn native-timed human siege engines toward their current attack order during reloading and before release. Defaults true; false retains the previous module behavior. Native cow orders and automatic target selection remain owned by their existing paths."
            },
            {
              "const": "native"
            }
          ],
          "description": "Turn native-timed human siege engines toward their current attack order during reloading and before release. Defaults true; false retains the previous module behavior. Native cow orders and automatic target selection remain owned by their existing paths. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults.",
          "default": true
        },
        "projectile": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "enum": [
                    "arrow",
                    "arrow_untargeted",
                    "ballista_bolt",
                    "catapult_rock",
                    "cow",
                    "crossbow_bolt",
                    "crossbow_bolt_untargeted",
                    "fire_arrow",
                    "fire_arrow_untargeted",
                    "fire_ballista_bolt",
                    "firethrower_pot",
                    "firethrower_pot_untargeted",
                    "mangonel_pebble",
                    "slinger_stone",
                    "slinger_stone_untargeted",
                    "trebuchet_rock",
                    1,
                    2,
                    3,
                    4,
                    7,
                    20,
                    23,
                    24,
                    25,
                    33,
                    34,
                    35,
                    36,
                    37,
                    91,
                    92
                  ]
                },
                {
                  "type": "string",
                  "pattern": "^[a-z][a-z0-9_-]{0,47}$"
                }
              ],
              "description": "Native projectile or a name defined in projectiles. The game loader rejects undefined variant names."
            },
            {
              "const": "native"
            }
          ],
          "description": "Native projectile or a name defined in projectiles. The game loader rejects undefined variant names. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "cow_projectile": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "enum": [
                    "arrow",
                    "arrow_untargeted",
                    "ballista_bolt",
                    "catapult_rock",
                    "cow",
                    "crossbow_bolt",
                    "crossbow_bolt_untargeted",
                    "fire_arrow",
                    "fire_arrow_untargeted",
                    "fire_ballista_bolt",
                    "firethrower_pot",
                    "firethrower_pot_untargeted",
                    "mangonel_pebble",
                    "slinger_stone",
                    "slinger_stone_untargeted",
                    "trebuchet_rock",
                    1,
                    2,
                    3,
                    4,
                    7,
                    20,
                    23,
                    24,
                    25,
                    33,
                    34,
                    35,
                    36,
                    37,
                    91,
                    92
                  ]
                },
                {
                  "type": "string",
                  "pattern": "^[a-z][a-z0-9_-]{0,47}$"
                }
              ],
              "description": "Separate replacement for native siege cow ammunition and automatic AI cow shots. Omission preserves cows."
            },
            {
              "const": "native"
            }
          ],
          "description": "Separate replacement for native siege cow ammunition and automatic AI cow shots. Omission preserves cows. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "targets": {
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "string",
                  "enum": [
                    "buildings",
                    "cluster",
                    "fortifications",
                    "siege_towers",
                    "units",
                    "walls"
                  ]
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "enum": [
                      "buildings",
                      "cluster",
                      "fortifications",
                      "siege_towers",
                      "units",
                      "walls"
                    ]
                  },
                  "minItems": 1,
                  "maxItems": 4,
                  "uniqueItems": true
                }
              ],
              "description": "Automatic search priorities. Cluster density applies only to AI owners. Human native shooters retain their current unit/building/ground/wall attack order within range; humans without an order treat cluster as units."
            },
            {
              "const": "native"
            }
          ],
          "description": "Automatic search priorities. Cluster density applies only to AI owners. Human native shooters retain their current unit/building/ground/wall attack order within range; humans without an order treat cluster as units. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "on_fortification": {
          "$ref": "#/$defs/override"
        },
        "near_decorations": {
          "type": "array",
          "maxItems": 33,
          "items": {
            "$ref": "#/$defs/decorationRule"
          }
        }
      },
      "dependentSchemas": {
        "stagger_max": {
          "anyOf": [
            {
              "required": [
                "interval"
              ]
            },
            {
              "required": [
                "interval_moving"
              ]
            },
            {
              "required": [
                "interval_standing"
              ]
            },
            {
              "required": [
                "attached_interval"
              ]
            }
          ]
        }
      },
      "allOf": [
        {
          "not": {
            "required": [
              "spread",
              "spread_tiles"
            ],
            "properties": {
              "spread": {
                "not": {
                  "const": "native"
                }
              },
              "spread_tiles": {
                "not": {
                  "const": "native"
                }
              }
            }
          }
        },
        {
          "not": {
            "required": [
              "inaccuracy",
              "inaccuracy_tiles"
            ],
            "properties": {
              "inaccuracy": {
                "not": {
                  "const": "native"
                }
              },
              "inaccuracy_tiles": {
                "not": {
                  "const": "native"
                }
              }
            }
          }
        }
      ],
      "dependentRequired": {
        "stagger_min": [
          "stagger_max"
        ]
      }
    },
    "override": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "attached_board_radius": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "attached_interval": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000,
              "description": "Enables firing for a siege tower docked to a wall, overriding moving/standing intervals; 0 holds fire. Omission keeps the current moving/standing rate or fallback."
            },
            {
              "const": "native"
            }
          ],
          "description": "Enables firing for a siege tower docked to a wall, overriding moving/standing intervals; 0 holds fire. Omission keeps the current moving/standing rate or fallback. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "count": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 64
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "cow_count": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 64
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "density_min": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 256,
              "description": "Minimum enemy count for AI cluster targeting; human attack orders are not restricted by this threshold."
            },
            {
              "const": "native"
            }
          ],
          "description": "Minimum enemy count for AI cluster targeting; human attack orders are not restricted by this threshold. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "density_radius": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 100,
              "description": "Radius in tiles for counting nearby enemies in an AI cluster."
            },
            {
              "const": "native"
            }
          ],
          "description": "Radius in tiles for counting nearby enemies in an AI cluster. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "inaccuracy": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 800,
              "description": "Maximum random aim-error radius in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Explicit 0 removes native random error; omission preserves it. Separate spread still applies. Do not combine with inaccuracy_tiles."
            },
            {
              "const": "native"
            }
          ],
          "description": "Maximum random aim-error radius in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Explicit 0 removes native random error; omission preserves it. Separate spread still applies. Do not combine with inaccuracy_tiles. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "inaccuracy_tiles": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer inaccuracy for native 1/8-tile steps. Do not combine both fields."
            },
            {
              "const": "native"
            }
          ],
          "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer inaccuracy for native 1/8-tile steps. Do not combine both fields. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "interval": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000,
              "description": "Optional fallback automatic-fire interval in simulation ticks. Applies only where no state interval overrides it."
            },
            {
              "const": "native"
            }
          ],
          "description": "Optional fallback automatic-fire interval in simulation ticks. Applies only where no state interval overrides it. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "interval_moving": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000,
              "description": "Enables firing while moving; 0 holds fire. If omitted, use interval, or hold fire if neither is set."
            },
            {
              "const": "native"
            }
          ],
          "description": "Enables firing while moving; 0 holds fire. If omitted, use interval, or hold fire if neither is set. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "interval_standing": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000,
              "description": "Enables firing while stopped, not specifically docked; 0 holds fire. If omitted, use interval, or hold fire if neither is set."
            },
            {
              "const": "native"
            }
          ],
          "description": "Enables firing while stopped, not specifically docked; 0 holds fire. If omitted, use interval, or hold fire if neither is set. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "preload_poll": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "range": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "require_manned": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 4
                },
                {
                  "type": "boolean"
                }
              ],
              "description": "Engineers currently aboard. Native reload defaults: trebuchet 3, other supported siege engines 2. Independent timer defaults 0. Explicit true means 1; false or 0 allows unmanned fire."
            },
            {
              "const": "native"
            }
          ],
          "description": "Engineers currently aboard. Native reload defaults: trebuchet 3, other supported siege engines 2. Independent timer defaults 0. Explicit true means 1; false or 0 allows unmanned fire. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "shoot_height": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 500
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "spread": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 800,
              "description": "Additional simultaneous-shot spread per axis, in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Zero adds no spread. Independent of inaccuracy."
            },
            {
              "const": "native"
            }
          ],
          "description": "Additional simultaneous-shot spread per axis, in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Zero adds no spread. Independent of inaccuracy. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "spread_tiles": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer spread for native 1/8-tile steps. Do not combine both fields."
            },
            {
              "const": "native"
            }
          ],
          "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer spread for native 1/8-tile steps. Do not combine both fields. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "stagger_max": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "stagger_min": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000,
              "description": "Requires stagger_max; must not exceed it when staggering is enabled. Checked by the game loader."
            },
            {
              "const": "native"
            }
          ],
          "description": "Requires stagger_max; must not exceed it when staggering is enabled. Checked by the game loader. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "sync_max_wait": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000,
              "description": "Legacy animation wait only; never bypasses a native reload firing frame."
            },
            {
              "const": "native"
            }
          ],
          "description": "Legacy animation wait only; never bypasses a native reload firing frame. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "wall_min_distance": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "ai_cow_vs_units": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "ai_only": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "attached_ignore_crew": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "attached_stop_when_boarded": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "auto_targeting": {
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "False requires an explicit human attack order and disables module automatic search and native automatic acquisition for this unit. True preserves native acquisition and permits configured interval searches. Native leaves the game unchanged when no interval is configured."
            },
            {
              "const": "native"
            }
          ],
          "description": "False requires an explicit human attack order and disables module automatic search and native automatic acquisition for this unit. True preserves native acquisition and permits configured interval searches. Native leaves the game unchanged when no interval is configured. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults.",
          "default": true
        },
        "preload": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "random_targets": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "strict_range": {
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Check automatic targets at native coordinate precision, including building centres. False restores the old rounded tile checks. Does not clamp projectile scatter or change native manual range rules."
            },
            {
              "const": "native"
            }
          ],
          "description": "Check automatic targets at native coordinate precision, including building centres. False restores the old rounded tile checks. Does not clamp projectile scatter or change native manual range rules. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults.",
          "default": true
        },
        "suppress_default": {
          "anyOf": [
            {
              "type": "boolean",
              "description": "Defaults true with an interval. False combines native and automatic shots only in independent timer mode; it cannot bypass native reload timing. Unchanged cow orders remain available."
            },
            {
              "const": "native"
            }
          ],
          "description": "Defaults true with an interval. False combines native and automatic shots only in independent timer mode; it cannot bypass native reload timing. Unchanged cow orders remain available. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "sync_to_animation": {
          "anyOf": [
            {
              "type": "boolean",
              "description": "Defaults to native firing-frame timing for catapult, trebuchet, mangonel, both ballistas, European/Arabian archers, horse archers, crossbowmen, slingers, firethrowers and hunters. Reload while waiting; never truncate the animation. Horse archers keep an independent bow clock while moving. Configured hunter shots use the native bow animation and retain other work states. False selects the independent timer. Other units default false; true uses the legacy bounded animation wait."
            },
            {
              "const": "native"
            }
          ],
          "description": "Defaults to native firing-frame timing for catapult, trebuchet, mangonel, both ballistas, European/Arabian archers, horse archers, crossbowmen, slingers, firethrowers and hunters. Reload while waiting; never truncate the animation. Horse archers keep an independent bow clock while moving. Configured hunter shots use the native bow animation and retain other work states. False selects the independent timer. Other units default false; true uses the legacy bounded animation wait. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "turn_before_shot": {
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Turn native-timed human siege engines toward their current attack order during reloading and before release. Defaults true; false retains the previous module behavior. Native cow orders and automatic target selection remain owned by their existing paths."
            },
            {
              "const": "native"
            }
          ],
          "description": "Turn native-timed human siege engines toward their current attack order during reloading and before release. Defaults true; false retains the previous module behavior. Native cow orders and automatic target selection remain owned by their existing paths. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults.",
          "default": true
        },
        "projectile": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "enum": [
                    "arrow",
                    "arrow_untargeted",
                    "ballista_bolt",
                    "catapult_rock",
                    "cow",
                    "crossbow_bolt",
                    "crossbow_bolt_untargeted",
                    "fire_arrow",
                    "fire_arrow_untargeted",
                    "fire_ballista_bolt",
                    "firethrower_pot",
                    "firethrower_pot_untargeted",
                    "mangonel_pebble",
                    "slinger_stone",
                    "slinger_stone_untargeted",
                    "trebuchet_rock",
                    1,
                    2,
                    3,
                    4,
                    7,
                    20,
                    23,
                    24,
                    25,
                    33,
                    34,
                    35,
                    36,
                    37,
                    91,
                    92
                  ]
                },
                {
                  "type": "string",
                  "pattern": "^[a-z][a-z0-9_-]{0,47}$"
                }
              ],
              "description": "Native projectile or a name defined in projectiles. The game loader rejects undefined variant names."
            },
            {
              "const": "native"
            }
          ],
          "description": "Native projectile or a name defined in projectiles. The game loader rejects undefined variant names. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "cow_projectile": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "enum": [
                    "arrow",
                    "arrow_untargeted",
                    "ballista_bolt",
                    "catapult_rock",
                    "cow",
                    "crossbow_bolt",
                    "crossbow_bolt_untargeted",
                    "fire_arrow",
                    "fire_arrow_untargeted",
                    "fire_ballista_bolt",
                    "firethrower_pot",
                    "firethrower_pot_untargeted",
                    "mangonel_pebble",
                    "slinger_stone",
                    "slinger_stone_untargeted",
                    "trebuchet_rock",
                    1,
                    2,
                    3,
                    4,
                    7,
                    20,
                    23,
                    24,
                    25,
                    33,
                    34,
                    35,
                    36,
                    37,
                    91,
                    92
                  ]
                },
                {
                  "type": "string",
                  "pattern": "^[a-z][a-z0-9_-]{0,47}$"
                }
              ],
              "description": "Separate replacement for native siege cow ammunition and automatic AI cow shots. Omission preserves cows."
            },
            {
              "const": "native"
            }
          ],
          "description": "Separate replacement for native siege cow ammunition and automatic AI cow shots. Omission preserves cows. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "targets": {
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "string",
                  "enum": [
                    "buildings",
                    "cluster",
                    "fortifications",
                    "siege_towers",
                    "units",
                    "walls"
                  ]
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "enum": [
                      "buildings",
                      "cluster",
                      "fortifications",
                      "siege_towers",
                      "units",
                      "walls"
                    ]
                  },
                  "minItems": 1,
                  "maxItems": 4,
                  "uniqueItems": true
                }
              ],
              "description": "Automatic search priorities. Cluster density applies only to AI owners. Human native shooters retain their current unit/building/ground/wall attack order within range; humans without an order treat cluster as units."
            },
            {
              "const": "native"
            }
          ],
          "description": "Automatic search priorities. Cluster density applies only to AI owners. Human native shooters retain their current unit/building/ground/wall attack order within range; humans without an order treat cluster as units. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        }
      },
      "description": "Sparse overrides while on a wall or fortification. Missing fields inherit the ground settings. Runtime validates the merged settings.",
      "allOf": [
        {
          "not": {
            "required": [
              "spread",
              "spread_tiles"
            ],
            "properties": {
              "spread": {
                "not": {
                  "const": "native"
                }
              },
              "spread_tiles": {
                "not": {
                  "const": "native"
                }
              }
            }
          }
        },
        {
          "not": {
            "required": [
              "inaccuracy",
              "inaccuracy_tiles"
            ],
            "properties": {
              "inaccuracy": {
                "not": {
                  "const": "native"
                }
              },
              "inaccuracy_tiles": {
                "not": {
                  "const": "native"
                }
              }
            }
          }
        }
      ]
    },
    "decorationRule": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "decoration"
      ],
      "minProperties": 2,
      "properties": {
        "attached_board_radius": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "attached_interval": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000,
              "description": "Enables firing for a siege tower docked to a wall, overriding moving/standing intervals; 0 holds fire. Omission keeps the current moving/standing rate or fallback."
            },
            {
              "const": "native"
            }
          ],
          "description": "Enables firing for a siege tower docked to a wall, overriding moving/standing intervals; 0 holds fire. Omission keeps the current moving/standing rate or fallback. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "count": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 64
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "cow_count": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 64
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "density_min": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 256,
              "description": "Minimum enemy count for AI cluster targeting; human attack orders are not restricted by this threshold."
            },
            {
              "const": "native"
            }
          ],
          "description": "Minimum enemy count for AI cluster targeting; human attack orders are not restricted by this threshold. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "density_radius": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 100,
              "description": "Radius in tiles for counting nearby enemies in an AI cluster."
            },
            {
              "const": "native"
            }
          ],
          "description": "Radius in tiles for counting nearby enemies in an AI cluster. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "inaccuracy": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 800,
              "description": "Maximum random aim-error radius in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Explicit 0 removes native random error; omission preserves it. Separate spread still applies. Do not combine with inaccuracy_tiles."
            },
            {
              "const": "native"
            }
          ],
          "description": "Maximum random aim-error radius in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Explicit 0 removes native random error; omission preserves it. Separate spread still applies. Do not combine with inaccuracy_tiles. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "inaccuracy_tiles": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer inaccuracy for native 1/8-tile steps. Do not combine both fields."
            },
            {
              "const": "native"
            }
          ],
          "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer inaccuracy for native 1/8-tile steps. Do not combine both fields. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "interval": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000,
              "description": "Optional fallback automatic-fire interval in simulation ticks. Applies only where no state interval overrides it."
            },
            {
              "const": "native"
            }
          ],
          "description": "Optional fallback automatic-fire interval in simulation ticks. Applies only where no state interval overrides it. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "interval_moving": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000,
              "description": "Enables firing while moving; 0 holds fire. If omitted, use interval, or hold fire if neither is set."
            },
            {
              "const": "native"
            }
          ],
          "description": "Enables firing while moving; 0 holds fire. If omitted, use interval, or hold fire if neither is set. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "interval_standing": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000,
              "description": "Enables firing while stopped, not specifically docked; 0 holds fire. If omitted, use interval, or hold fire if neither is set."
            },
            {
              "const": "native"
            }
          ],
          "description": "Enables firing while stopped, not specifically docked; 0 holds fire. If omitted, use interval, or hold fire if neither is set. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "preload_poll": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "range": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "require_manned": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 4
                },
                {
                  "type": "boolean"
                }
              ],
              "description": "Engineers currently aboard. Native reload defaults: trebuchet 3, other supported siege engines 2. Independent timer defaults 0. Explicit true means 1; false or 0 allows unmanned fire."
            },
            {
              "const": "native"
            }
          ],
          "description": "Engineers currently aboard. Native reload defaults: trebuchet 3, other supported siege engines 2. Independent timer defaults 0. Explicit true means 1; false or 0 allows unmanned fire. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "shoot_height": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 500
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "spread": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 800,
              "description": "Additional simultaneous-shot spread per axis, in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Zero adds no spread. Independent of inaccuracy."
            },
            {
              "const": "native"
            }
          ],
          "description": "Additional simultaneous-shot spread per axis, in whole native coordinate units: 1 = 1/8 tile, 8 = 1 tile. Zero adds no spread. Independent of inaccuracy. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "spread_tiles": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer spread for native 1/8-tile steps. Do not combine both fields."
            },
            {
              "const": "native"
            }
          ],
          "description": "Compatibility alias in whole tiles, converted by multiplying by 8. Prefer spread for native 1/8-tile steps. Do not combine both fields. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "stagger_max": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 60000
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "stagger_min": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000,
              "description": "Requires stagger_max; must not exceed it when staggering is enabled. Checked by the game loader."
            },
            {
              "const": "native"
            }
          ],
          "description": "Requires stagger_max; must not exceed it when staggering is enabled. Checked by the game loader. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "sync_max_wait": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1,
              "maximum": 60000,
              "description": "Legacy animation wait only; never bypasses a native reload firing frame."
            },
            {
              "const": "native"
            }
          ],
          "description": "Legacy animation wait only; never bypasses a native reload firing frame. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "wall_min_distance": {
          "anyOf": [
            {
              "type": "integer",
              "minimum": 0,
              "maximum": 100
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "ai_cow_vs_units": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "ai_only": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "attached_ignore_crew": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "attached_stop_when_boarded": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "auto_targeting": {
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "False requires an explicit human attack order and disables module automatic search and native automatic acquisition for this unit. True preserves native acquisition and permits configured interval searches. Native leaves the game unchanged when no interval is configured."
            },
            {
              "const": "native"
            }
          ],
          "description": "False requires an explicit human attack order and disables module automatic search and native automatic acquisition for this unit. True preserves native acquisition and permits configured interval searches. Native leaves the game unchanged when no interval is configured. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults.",
          "default": true
        },
        "preload": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "random_targets": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "const": "native"
            }
          ],
          "description": " Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "strict_range": {
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Check automatic targets at native coordinate precision, including building centres. False restores the old rounded tile checks. Does not clamp projectile scatter or change native manual range rules."
            },
            {
              "const": "native"
            }
          ],
          "description": "Check automatic targets at native coordinate precision, including building centres. False restores the old rounded tile checks. Does not clamp projectile scatter or change native manual range rules. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults.",
          "default": true
        },
        "suppress_default": {
          "anyOf": [
            {
              "type": "boolean",
              "description": "Defaults true with an interval. False combines native and automatic shots only in independent timer mode; it cannot bypass native reload timing. Unchanged cow orders remain available."
            },
            {
              "const": "native"
            }
          ],
          "description": "Defaults true with an interval. False combines native and automatic shots only in independent timer mode; it cannot bypass native reload timing. Unchanged cow orders remain available. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "sync_to_animation": {
          "anyOf": [
            {
              "type": "boolean",
              "description": "Defaults to native firing-frame timing for catapult, trebuchet, mangonel, both ballistas, European/Arabian archers, horse archers, crossbowmen, slingers, firethrowers and hunters. Reload while waiting; never truncate the animation. Horse archers keep an independent bow clock while moving. Configured hunter shots use the native bow animation and retain other work states. False selects the independent timer. Other units default false; true uses the legacy bounded animation wait."
            },
            {
              "const": "native"
            }
          ],
          "description": "Defaults to native firing-frame timing for catapult, trebuchet, mangonel, both ballistas, European/Arabian archers, horse archers, crossbowmen, slingers, firethrowers and hunters. Reload while waiting; never truncate the animation. Horse archers keep an independent bow clock while moving. Configured hunter shots use the native bow animation and retain other work states. False selects the independent timer. Other units default false; true uses the legacy bounded animation wait. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "turn_before_shot": {
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Turn native-timed human siege engines toward their current attack order during reloading and before release. Defaults true; false retains the previous module behavior. Native cow orders and automatic target selection remain owned by their existing paths."
            },
            {
              "const": "native"
            }
          ],
          "description": "Turn native-timed human siege engines toward their current attack order during reloading and before release. Defaults true; false retains the previous module behavior. Native cow orders and automatic target selection remain owned by their existing paths. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults.",
          "default": true
        },
        "projectile": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "enum": [
                    "arrow",
                    "arrow_untargeted",
                    "ballista_bolt",
                    "catapult_rock",
                    "cow",
                    "crossbow_bolt",
                    "crossbow_bolt_untargeted",
                    "fire_arrow",
                    "fire_arrow_untargeted",
                    "fire_ballista_bolt",
                    "firethrower_pot",
                    "firethrower_pot_untargeted",
                    "mangonel_pebble",
                    "slinger_stone",
                    "slinger_stone_untargeted",
                    "trebuchet_rock",
                    1,
                    2,
                    3,
                    4,
                    7,
                    20,
                    23,
                    24,
                    25,
                    33,
                    34,
                    35,
                    36,
                    37,
                    91,
                    92
                  ]
                },
                {
                  "type": "string",
                  "pattern": "^[a-z][a-z0-9_-]{0,47}$"
                }
              ],
              "description": "Native projectile or a name defined in projectiles. The game loader rejects undefined variant names."
            },
            {
              "const": "native"
            }
          ],
          "description": "Native projectile or a name defined in projectiles. The game loader rejects undefined variant names. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "cow_projectile": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "enum": [
                    "arrow",
                    "arrow_untargeted",
                    "ballista_bolt",
                    "catapult_rock",
                    "cow",
                    "crossbow_bolt",
                    "crossbow_bolt_untargeted",
                    "fire_arrow",
                    "fire_arrow_untargeted",
                    "fire_ballista_bolt",
                    "firethrower_pot",
                    "firethrower_pot_untargeted",
                    "mangonel_pebble",
                    "slinger_stone",
                    "slinger_stone_untargeted",
                    "trebuchet_rock",
                    1,
                    2,
                    3,
                    4,
                    7,
                    20,
                    23,
                    24,
                    25,
                    33,
                    34,
                    35,
                    36,
                    37,
                    91,
                    92
                  ]
                },
                {
                  "type": "string",
                  "pattern": "^[a-z][a-z0-9_-]{0,47}$"
                }
              ],
              "description": "Separate replacement for native siege cow ammunition and automatic AI cow shots. Omission preserves cows."
            },
            {
              "const": "native"
            }
          ],
          "description": "Separate replacement for native siege cow ammunition and automatic AI cow shots. Omission preserves cows. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "targets": {
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "string",
                  "enum": [
                    "buildings",
                    "cluster",
                    "fortifications",
                    "siege_towers",
                    "units",
                    "walls"
                  ]
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "enum": [
                      "buildings",
                      "cluster",
                      "fortifications",
                      "siege_towers",
                      "units",
                      "walls"
                    ]
                  },
                  "minItems": 1,
                  "maxItems": 4,
                  "uniqueItems": true
                }
              ],
              "description": "Automatic search priorities. Cluster density applies only to AI owners. Human native shooters retain their current unit/building/ground/wall attack order within range; humans without an order treat cluster as units."
            },
            {
              "const": "native"
            }
          ],
          "description": "Automatic search priorities. Cluster density applies only to AI owners. Human native shooters retain their current unit/building/ground/wall attack order within range; humans without an order treat cluster as units. Native explicitly leaves this field unmodified; configured automatic fire still has its documented module defaults."
        },
        "decoration": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_-]{0,47}$"
        }
      },
      "allOf": [
        {
          "not": {
            "required": [
              "spread",
              "spread_tiles"
            ],
            "properties": {
              "spread": {
                "not": {
                  "const": "native"
                }
              },
              "spread_tiles": {
                "not": {
                  "const": "native"
                }
              }
            }
          }
        },
        {
          "not": {
            "required": [
              "inaccuracy",
              "inaccuracy_tiles"
            ],
            "properties": {
              "inaccuracy": {
                "not": {
                  "const": "native"
                }
              },
              "inaccuracy_tiles": {
                "not": {
                  "const": "native"
                }
              }
            }
          }
        }
      ],
      "description": "First matching rule wins within the native brazier 3-tile square and height difference below 45. Sparse fields apply after fortification overrides. Names must exist in decorations."
    }
  }
}
